let ApolloError,verify,JWT_SECRET,User;_a26‍.x([["default",()=>_a26‍.o]]);_a26‍.w("apollo-server-express",[["ApolloError",["ApolloError"],function(v){ApolloError=v}]]);_a26‍.w("jsonwebtoken",[["verify",["verify"],function(v){verify=v}]]);_a26‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_a26‍.w("../models",[["User",["User"],function(v){User=v}]]);



const AuthMiddleware = async (req, res, next) => {
  const authHeader = req.get("Authorization");

  try {
    if (authHeader) {
      let token = authHeader.split(" ")[1];
      if (!token || token === "") {
        req.isAuth = false;
        next();
      } else {
        let decoded_Token = verify(token, JWT_SECRET);
        if (decoded_Token) {
          let authuser = await User.findById(decoded_Token.user._id);

          if (authuser) {
            req.user = authuser;
            req.isAuth = true;
            next();
          } else {
            req.isAuth = false;
            next();
          }
        } else {
          req.isAuth = false;
          next();
        }
      }
    } else {
      req.isAuth = false;
      next();
    }
  } catch (error) {
    throw new ApolloError(error.message, 401);
  }
};

_a26‍.d(AuthMiddleware);
